src/
│
├── config/         → Cấu hình (database, cloud, env, etc.)
├── controllers/    → Xử lý logic request từ client
├── middlewares/    → Xử lý giữa request và controller (auth, validate,…)
├── models/         → Định nghĩa schema MongoDB (User, Post, Share,…)
├── routes/         → Định nghĩa endpoint API (nối tới controller)
├── services/       → Xử lý nghiệp vụ phức tạp, tách khỏi controller
├── utils/          → Hàm tiện ích, helper chung (format date, random,…)
├── app.js          → Cấu hình app Express (middleware, routes,…)
└── server.js       → File chạy chính, khởi động server

config/
├── db.js            → Kết nối MongoDB
├── cloudinary.js    → Cấu hình upload ảnh
├── jwt.js           → Sinh / verify token JWT
└── env.js           → Đọc biến môi trường .env

controllers/
├── user.controller.js
├── post.controller.js
└── share.controller.js

middlewares/
├── auth.middleware.js      → Xác thực JWT
├── error.middleware.js     → Bắt lỗi tập trung
└── role.middleware.js      → Kiểm tra quyền (admin, vendor,…)

models/
├── User.js
├── Post.js
├── Share.js
├── Comment.js
└── Notification.js

routes/
├── user.route.js
├── post.route.js
└── share.route.js

services/
├── notification.service.js  → Gửi thông báo khi share bài
└── upload.service.js        → Upload ảnh/video

utils/
├── dateFormatter.js
├── randomString.js
└── response.js

src/
│
├── controllers/
│   ├── userController.js
│   ├── authController.js
│
├── models/
│   ├── userModel.js
│   ├── productModel.js
│
├── routes/
│   ├── userRoute.js
│   ├── authRoute.js
│
├── middlewares/
│   ├── authMiddleware.js
│   ├── errorHandler.js
│
├── utils/
│   ├── dateFormatter.js
│   ├── sendMail.js
│
└── validators/
    ├── userValidator.js
    ├── productValidator.js
    └── authValidator.js
